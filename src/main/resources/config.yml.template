vertx:
  redis:
    host: localhost
    port: 6379
  proxy:
    type: socks5
    host: localhost
    port: 1080

accounts:
  - type: fcoin
    id: fcoin_main
    baseCurrency: btc
    quoteCurrency: usdt
    key:
    secret:

  - type: fcoin
    id: fcoin_shadow
    baseCurrency: btc
    quoteCurrency: usdt
    key:
    secret:

traders:
  - class: xyz.homebrew.trader.AlgowTrader
    accounts:
      - &fcoin_main
      - &fcoin_shadow
